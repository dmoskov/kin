// Part of the family-tree web app. Loaded as an ordered classic script.
// See index.html for load order. Split from the former monolithic app.js.

// Bootstrap: runs after every module above has loaded and defined its
// functions (these calls rely on definitions spread across all parts).
initTheme();
init();
